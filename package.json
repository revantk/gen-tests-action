{
  "name": "gen-tests-action",
  "version": "1.0.0",
  "description": "Github action for generating unit tests automatically. Currently works only for Python codebases.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1"
  }
}
